  1. Organized Model Weights & Git Ignored Binary Files:
      • Moved the base  yolo11n-pose.pt  and the trained  best.pt / last.pt  into the  weights/  directory.
      • Placed the production  best.onnx  model inside best.onnx.
      • Updated .gitignore to exclude all  .pt  binary weights ( weights/*.pt ) and validation overlays (
      simulation/runs/detections/ ), while tracking the  .onnx  model.
  2. Recorded Training Logs & Metrics:
      • Copied training graphs and loss data ( results.csv  and  results.png ) into .
  3. Validated on Raw UZH-FPV Images:
      • Created test_on_uzh.py, which runs the model on the raw DAVIS240C camera frames.
      • Logged all quantitative results, bounding box corners, and confidence scores into uzh_test_results.txt.
      • Successfully verified Sim-to-Real Transfer with gate detections on the real FPV frames showing up to 78.3% confidence.
  4. Documented & Committed:
      • Fully updated the README.md with details of the synthetic data projection math, training parameters, validation results,
      and execution guides.
      • Staged and committed all deliverables to the git repository.
